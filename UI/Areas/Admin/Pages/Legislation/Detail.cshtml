@page "{id:int}"
@model UI.Areas.Admin.Pages.Legislation.Detail

@{
    ViewData["Title"] = Model.Legislation.Title + " Detay";
}

<div class="container col-md-8">
    <section>
        <div class="container section-padding pb-0">
            <div id="blog-detail">
                <div class="blog-header mb-4">
                    <h1 class="mb-0 date">@Model.Legislation.Title</h1>
                    <div class="text-muted">
                        <i class="far fa-calendar-alt mr-2"></i>
                        @Model.Legislation.CreationDate.ToShortDateString()
                    </div>
                </div>
                <p>
                    @Html.Raw(Model.Legislation.Content)
                </p>
            </div>
            <div class="row m-0">
                <a href="/admin/legislation" class="btn btn-secondary ml-auto">Geri Dön</a>
                <a href="/admin/legislation/edit/@Model.Legislation.Id" class="btn btn-success ml-2">
                    Düzenle
                </a>
            </div>
        </div>
    </section>
</div>